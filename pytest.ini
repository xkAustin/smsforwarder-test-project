[pytest]
pythonpath = .
testpaths = tests
addopts = -ra
markers =
    e2e: end-to-end tests (require running emulator + SmsForwarder configured)
    performance: performance tests (run with -m performance)
    manual: requires manual trigger (run with -m manual)
